/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['emova.com.ar', 'i.imgur.com'], // Add your external domains here
    },
};

export default nextConfig;